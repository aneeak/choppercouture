# Node
node_modules/
.next/
out/
.turbo/

# Editor
.DS_Store

# Env
.env.local
.env.*.local

# Große Backups (nie ins Repo)
public/videos/process_original.mp4

# Master-Video-Exporte im Projekt-Root (60 MB / 159 MB).
# GitHub lehnt Dateien über 100 MB ab — die gehören nicht ins Repo.
/CC_Landing_Vid_mobile.mp4
/CC_Prozess_mobile.mp4

# InDesign-Font-Listen (werden von InDesign automatisch erzeugt)
indesign/**/AdobeFnt*.lst

# Galerie-Quellbilder (nur die optimierten in public/images/galerie/)
CC_Galerie_Bilder/

# Claude runtime
.claude/

# Generierte Bildvarianten. Werden vor jedem Build neu erzeugt
# (npm-Script "prebuild"), gehören deshalb nicht ins Repo.
public/images/_r/

# Bilder, die auf der Seite nirgends eingebunden sind. Liegen hier nur
# als Reserve, werden nicht deployed. Die Historie hat sie weiterhin.
archive/

# TypeScript-Build-Info, wird bei jedem Build neu erzeugt
tsconfig.tsbuildinfo
